{  
  "name": "form-ITBI",  
  "version": "1.0.0",  
  "description": "Form inteligente para ITBI",  
  "main": "index.js",  
  "scripts": {  
    "test": "echo \"No tests specified\" && exit 1",  
    "start": "node index.js"  // Adjust the entry point if needed  
  },  
  "author": "DTI",  
  "license": "MIT",  // Change as necessary  
  "dependencies": {  
    "nodemailer": "*"  // Adjust the version as needed  
  },  
  "engines": {  
    "node": ">=20.0.0"  
  }  
}
