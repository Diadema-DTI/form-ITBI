[build]
  functions = "functions"

[functions]
  node_bundler = "esbuild"

[build.environment]
  GOOGLE_SPREADSHEET_ID = "1WrHqsqQZ1CM8nsF_SfQUEEwtwhz-ZOzx"
  # Adicione outras variáveis de ambiente aqui, mas não inclua valores sensíveis

[template.environment]
  SMTP_HOST = "smtp.gmail.com"
  SMTP_PORT = "465"
  SMTP_FROM = "dti@diadema.sp.gov.br"
  LAST_PROTOCOL = 0
